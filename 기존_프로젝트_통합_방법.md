# 🔗 기존 프로젝트 통합 가이드

## 현재 상황

✅ **기존 프로젝트**: `accounting-ledger-doozon` 폴더에 이미 존재
- `AdvancedLedgerAnalysis.tsx` - 메인 분석 페이지
- `SamplingAnalysis.tsx` - 샘플링 분석 페이지
- 기타 여러 분석 페이지들...

✅ **새로 통합된 컴포넌트**: Google AI Studio에서 가져온 파일들
- `src/components/AIInsights.tsx` - AI 분석 컴포넌트
- `src/components/Dashboard.tsx` - 대시보드 컴포넌트
- `src/components/JournalTable.tsx` - 분개장 테이블
- `src/components/UploadZone.tsx` - 파일 업로드
- `src/services/geminiAnalysisService.ts` - AI 분석 서비스

---

## 💡 통합 방법 옵션

### 옵션 1: 기존 페이지에 통합 (추천 ⭐)

`AdvancedLedgerAnalysis.tsx` 페이지에 새 컴포넌트들을 추가하는 방법

**장점:**
- 기존 기능과 함께 사용 가능
- 통합된 UI 제공
- 사용자가 한 곳에서 모든 기능 접근

**예시:**
```typescript
// AdvancedLedgerAnalysis.tsx에 추가
import Dashboard from '@/components/Dashboard';
import JournalTable from '@/components/JournalTable';
import AIInsights from '@/components/AIInsights';

// 기존 코드에 새 컴포넌트들을 추가
```

---

### 옵션 2: 새 페이지로 추가

별도의 새 페이지를 만들어서 통합하는 방법

**장점:**
- 기존 코드와 분리
- 독립적으로 관리 가능
- 필요시 별도 라우팅

**예시:**
```typescript
// src/pages/AIAnalysis.tsx 새로 생성
// App.tsx에 새 라우트 추가: /ai-analysis
```

---

### 옵션 3: 탭/뷰로 추가

기존 페이지의 새로운 뷰(탭)로 추가하는 방법

**장점:**
- 기존 UI 구조 유지
- 탭으로 기능 분리
- 사용자 선택 가능

**예시:**
```typescript
// AdvancedLedgerAnalysis.tsx의 currentView에 추가
type View = 'selection' | 'account_analysis' | ... | 'ai_insights' | 'dashboard';
```

---

## 🎯 추천 방법: 옵션 3 (탭으로 추가)

기존 프로젝트가 이미 뷰(View) 시스템을 사용하고 있으므로, 새 기능을 뷰로 추가하는 것이 가장 자연스럽습니다.

### 구현 방법

1. **분석 메뉴에 새 옵션 추가**
   ```typescript
   // AdvancedLedgerAnalysis.tsx의 analysisOptions에 추가
   { 
     id: 'ai_insights', 
     title: 'AI 심층 분석', 
     description: 'Google AI Studio에서 가져온 AI 분석 기능',
     icon: Sparkles 
   },
   { 
     id: 'dashboard', 
     title: '대시보드', 
     description: '통계 차트 및 요약 대시보드',
     icon: BarChart3 
   },
   ```

2. **View 타입에 추가**
   ```typescript
   type View = 'selection' | ... | 'ai_insights' | 'dashboard';
   ```

3. **새 뷰 렌더링 추가**
   ```typescript
   if (currentView === 'ai_insights') {
     // JournalEntry로 변환 후 AIInsights 컴포넌트 사용
   }
   if (currentView === 'dashboard') {
     // Dashboard 컴포넌트 사용
   }
   ```

---

## 📋 필요한 작업

### 1. 데이터 변환 준비

기존 `LedgerRow` 데이터를 `JournalEntry`로 변환하는 작업:

```typescript
import { convertLedgerRowsToJournalEntries } from '@/services/geminiAnalysisService';

// AdvancedLedgerAnalysis.tsx에서
const journalEntries = useMemo(() => {
  if (!selectedAccount || currentAccountData.length === 0) return [];
  
  const headers = Object.keys(currentAccountData[0] || {});
  return convertLedgerRowsToJournalEntries(currentAccountData, headers);
}, [currentAccountData, selectedAccount]);
```

### 2. 컴포넌트 통합

기존 페이지에 새 컴포넌트 import 및 사용

### 3. 라우팅 추가 (옵션 2인 경우)

`App.tsx`에 새 라우트 추가

---

## 🚀 바로 시작하기

어떤 방법으로 통합할지 선택해주세요:

1. ✅ **기존 페이지에 탭으로 추가** (옵션 3 - 추천)
2. ✅ **기존 페이지에 직접 통합** (옵션 1)
3. ✅ **새 페이지로 추가** (옵션 2)
4. ✅ **일단 그대로 두고 나중에 통합** (선택지 유지)

선택해주시면 바로 통합 작업을 진행하겠습니다! 🎉


