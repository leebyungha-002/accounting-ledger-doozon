# 어제 작업 복구 가이드

## ⚠️ 현재 상황
- 19개 파일이 수정되었지만 Git에 커밋되지 않았습니다
- 많은 새 파일이 추가되었습니다
- 최근 커밋: "API Key 설정 및 테스트 기능 추가" (2025-11-25)

## ✅ 복구 방법 (안전한 순서)

### 1단계: 현재 작업 백업
현재 변경사항을 임시 저장합니다:

```powershell
cd "C:\Users\USer\Documents\accounting-ledger-doozon"
git stash push -u -m "2025-11-26 백업"
```

### 2단계: 이전 커밋 상태로 복구
Git에 커밋된 마지막 상태로 되돌립니다:

```powershell
git reset --hard HEAD
git clean -fd  # 추적되지 않은 새 파일들도 삭제
```

### 3단계: 필요시 백업에서 복구
백업한 내용 중 필요한 부분만 복구:

```powershell
# 백업 목록 확인
git stash list

# 특정 파일만 복구 (예: AIInsights.tsx)
git checkout stash@{0} -- src/components/AIInsights.tsx
```

## 🔄 대안: Cursor 에디터의 로컬 히스토리 사용

1. **파일별 히스토리 확인**
   - Cursor에서 파일 탭에서 오른쪽 클릭
   - "Timeline" 또는 "Local History" 선택
   - 원하는 시점 선택

2. **Ctrl+Shift+P** → "Timeline" 또는 "Local History" 검색

3. **파일 탐색기에서**
   - 파일에서 오른쪽 클릭
   - "Open Timeline" 선택

## 📋 현재 수정된 주요 파일들
- src/lib/geminiClient.ts
- src/pages/AdvancedLedgerAnalysis.tsx
- src/components/ui/dialog.tsx
- src/index.css
- 기타 15개 파일

## 💡 추천 방법
**안전을 위해 먼저 백업 후 복구하세요!**

1. 현재 변경사항을 stash로 백업
2. 이전 커밋 상태로 복구
3. 필요한 파일만 선택적으로 복구

